R-studio

Click Insert R to insert R code
```{r include=FALSE}
require(mosaic)
require(tigerstats)
```

require():using package
include = FALSE: don't show the import package

```{r echo = FALSE}
```
echo = FALSE: don't show the code but still show the output

# then space : Title
## then space : Section
> block quote. Can use tab before that
*text* : Italic
** text **: bold
* then space :bullet
four space then *: sub bullet
$H_1$: math for subscript

```{r fig.width = 4,fig.height = 3}
```

Install package:
install.packages("arules")


iris %>% head() %>% summary()
Thus iris %>% head() %>% summary() is equivalent to summary(head(iris)).
Some people prefer chaining to nesting because the functions applied can be read
from left to right rather than from inside out.

%in% is a more intuitive interface as a binary operator,
which returns a logical vector indicating if there is a match or not for the items 
in the itemsets (left operand) with the items in the table (right operand).
